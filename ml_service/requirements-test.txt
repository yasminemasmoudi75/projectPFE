# Dependencies for running tests (without pyodbc SQL Server driver)
# Use this for testing on Windows without Visual C++ Build Tools

pandas==2.3.2
numpy==2.3.3
scikit-learn>=1.3.0
joblib==1.4.2
Flask==3.0.0
Flask-CORS==4.0.0
python-dotenv==1.0.0
python-json-logger==2.0.7
pytest==7.4.3
pytest-cov==4.1.0

# Optional for production deployment
# Gunicorn==21.2.0
# pyodbc==4.0.39  <- Requires Visual C++ Build Tools on Windows
